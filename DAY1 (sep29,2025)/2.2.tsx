// This is TypeScript-specific syntax
let message: string = "Hello, World!"; // We've added ': string'

console.log(message);

// Now, let's try to do something that would work in JavaScript but fails in TypeScript
// message = 100; // This line will cause a compile-time error in TypeScript